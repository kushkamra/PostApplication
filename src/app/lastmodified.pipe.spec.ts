import { LastmodifiedPipe } from './lastmodified.pipe';

describe('LastmodifiedPipe', () => {
  it('create an instance', () => {
    const pipe = new LastmodifiedPipe();
    expect(pipe).toBeTruthy();
  });
});
